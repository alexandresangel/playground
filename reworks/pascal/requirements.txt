fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
python-multipart>=0.0.9
openai>=1.42.0
httpx>=0.27.0
pyjwt>=2.8.0
cryptography>=42.0.0
azure-storage-blob>=12.19.0
azure-identity>=1.15.0

# OpenTelemetry (logs + traces via OTLP)
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http
opentelemetry-instrumentation-logging


# Workflow orchestration; keep the existing AzureOpenAI and custom MCP clients.
langgraph>=1.2.4,<2

# Windows development needs the IANA database used by the unchanged timezone logic.
tzdata; sys_platform == "win32"
